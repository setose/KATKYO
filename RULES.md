# 運用ルール

資料が増えていく前提で、管理しやすくするためのルールです。AIに資料を作らせる時もこのルールを踏まえて指示してください。

## 1. ファイルの置き場所

- 教科フォルダの直下に置く。単元が増えて煩雑になってきたら、`高校数学/数2/点と直線/` のように単元名でサブフォルダを切る
- 新しい教科が増えたらトップに新しいフォルダを作る（`build_index.py` が自動で拾うので特別な登録作業は不要）
- あらかじめ用意してあるフォルダ構成は以下の通り（中身が空のフォルダは `.gitkeep` だけ入っている。ファイルを置いたら `.gitkeep` は消してよい）

```
中学国語/1年, 2年, 3年
中学数学/1年, 2年, 3年
中学英語/1年, 2年, 3年
中学理科/1年, 2年, 3年
中学社会/1年, 2年, 3年

高校数学/数1, 数A, 数2, 数B, 数3, 数C
高校物理/            （既存・トップレベル単独）
高校理科/化学, 生物, 地学
高校社会/日本史, 世界史, 地理, 公共, 倫理, 政治経済
高校古文/            （既存・トップレベル単独）
高校現代文/
高校漢文/
高校英語/
```

- `高校物理` と `高校古文` は既にファイルが入っているため、他の理科・国語科目とは違いトップレベルに単独で残してある（`高校理科/物理` や `高校国語/古文` へ後から統合してもよい）
- ここに無い単元・学年・科目が必要になったら、都度同じ命名パターンで追加してよい（真似すれば揃う）

## 2. ファイル名

- 内容が一目でわかる日本語名にする（例: `2次関数_平行移動.html`）
- 半角スペース・カンマ・丸括弧は使わない（URLでエスケープされて扱いにくくなるため）
- シリーズ物は `単元名_1.html` `単元名_2.html` のようにアンダースコアで連番を付ける
- `code_artifact.html` のような、AIが自動生成したデフォルトのファイル名のまま置かない。必ず内容に合わせてリネームする

## 3. HTMLファイルの中身

新しいHTMLをAIに作らせるときは、次の点を必ず満たすように指示する。

- `<meta charset="UTF-8">` と `<meta name="viewport" content="width=device-width, initial-scale=1.0">` を入れる
- `<title>` に**そのページの内容が一覧に出しても分かる短いタイトル**を入れる（index.htmlの一覧はこの`<title>`をそのまま表示するため）
    - 悪い例: `複素数平面 問3 解説`（「問3」だけでは何の問題か分からず、一覧に出しても他と見分けがつかない）
    - 良い例: `複素数平面：ド・モアブルの定理の使い方`
- タイトルの先頭に `③` や `【初心者向け】` のような装飾・連番は付けない（一覧表示が揃わなくなるため）

## 4. index.html は手動で編集しない

`index.html` は **`build_index.py` が自動生成するファイル**です。直接編集しても、次にスクリプトを実行すると上書きされます。

資料を追加・削除・リネームしたら、リポジトリのルートで以下を実行してください。

```bash
python build_index.py
```

実行すると、フォルダ構成と各HTMLの`<title>`を読み取って `index.html` を再生成します。教科の表示順は `build_index.py` 内の `SUBJECT_ORDER` に書いてあり、リストに無い新しい教科フォルダは自動的に末尾（五十音順）に追加されます。

## 5. コミットの流れ

1. 資料のHTMLを追加・修正・リネームする
2. `python build_index.py` を実行して index.html を更新する
3. `git add -A` してコミットする（index.htmlの差分も一緒にコミットする）

## 6. 重複ファイルを作らない

- 同じ内容のファイルを `xxx.html` と `xxxre.html` のような紛らわしい名前で残さない
- 修正するときは新しいファイルを増やすのではなく、既存ファイルを上書き修正する（履歴はgitが持っているので、バージョン違いのファイルを並べて残す必要はない）

## 7. 特定の生徒向けの個別資料はリポジトリに入れない

このリポジトリ（`KATKYO/`）はGitHub Pages / Cloudflare Pagesで公開する前提のため、**特定の生徒の1問だけを解説したような個別資料は置かない**。

判断の目安:

- 「〇〇さんが持ってきた問題」「画像にある問題」「今回の問題」のように、特定の1問・1枚の写真に紐づいている → 個別資料
- 概念や解法パターンの説明で、（演習問題が付いていても）どの生徒にも使い回せる → 一般教材（公開してよい）

個別資料は、リポジトリと同じ階層にある `../private/`（`KATEKYOhtml/private/`）に、教科フォルダ構成を保ったまま移動する。このフォルダはGitリポジトリの外にあるため、誤って公開される心配がない。

```
KATEKYOhtml/
├── KATKYO/     ← 公開用リポジトリ（一般教材のみ）
└── private/    ← 個別資料の置き場所（Git管理外）
```

新しくAIに資料を作らせたときは、公開前にこの基準でチェックし、該当するものは `KATKYO/` ではなく `private/` に保存する。
